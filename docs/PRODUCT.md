# Product

`UnEmployed` is one local-first desktop app with two modules:

- `Job Finder`
- `Interview Helper`

## Shared Baseline

- shared local profile, documents, reusable answers, and application history
- shared browser runtime, source intelligence, AI provider roles, desktop shell, settings, tray, and hotkeys
- UI-first workflows with agent assistance inside bounded product surfaces

Eight consistency principles govern every Job Finder surface. They generalize
the defects real users reported, so a new screen is judged against the
principle, not against whether that exact case was reported before:

- **One component per job.** One tab style, one list-row style, one badge,
  chip, and count treatment, one card, one toolbar control height, one button
  hierarchy, one empty state, one dialog and close affordance. A second visual
  implementation of the same job is a defect, and a guard test pins the
  canonical one.
- **Nothing shifts.** Opening or closing a panel, selecting a row, hovering,
  loading, resizing, or a toast never moves or resizes unrelated content.
  Floating surfaces overlay; they never reflow. A skeleton is laid out block
  for block like the loaded screen.
- **Everything in reach.** At every supported size down to 1024x720, and in
  short windows, a screen's primary action is visible without scrolling.
  Floating surfaces stay inside the window and never cover the primary
  action, a tab strip, or a composer; popovers fit or scroll internally.
- **Aligned and even.** Controls in one row share one height, radius, and
  border token; groups align to one axis; the header is centred; gutters are
  identical across routes.
- **One fact, one voice.** A number or state shown on two screens uses the
  same noun and the same population. No raw error strings, identifiers, or
  internal vocabulary reach a user surface, and no label promises something
  the app cannot do.
- **Legible.** Text is at least 4.5:1 and boundaries and states at least 3:1
  in both themes; disabled looks disabled; links look like links.
- **Use the room.** No dropdown or disclosure hides what the space already
  shows; no empty columns; no oversized hero above a small next action.
- **Honest loading.** Every wait shows elapsed and expected time or a real
  progress signal, never a fabricated percentage, and loading states are laid
  out like the loaded state.
- one published type scale rather than a per-screen one: headings render at 24 / 19 / 16 / 14px for h1-h4, all semibold and strictly decreasing, so a subsection heading can never render larger or heavier than the section heading above it; text typed into any field renders at body size instead of inheriting its container's size; no label, chip, badge, or table header renders below 11px; and the page title is a fixed size, so it does not change between routes; an eyebrow or kicker label — the small uppercase word above a card's real title — is a styled non-heading element that names its region through `aria-labelledby`, never a heading tag, so the document outline a screen reader walks matches the visual hierarchy instead of announcing a 14px badge as a section heading
- one contrast and state floor: any control whose only boundary is a border clears the 3:1 non-text floor against both the panel and the page canvas in both themes, a disabled control loses its fill rather than differing only by label colour, text-only controls are always underlined so hue is never the only signal that something is clickable, scrollbar thumbs are visible because on a clipped screen the scrollbar is often the only "there is more" cue, and attention states that are not failures carry their own warning tone instead of borrowing the failure hue
- one breakpoint-precedence rule in the renderer: Tailwind v4 emits arbitrary `min-[…]` / `max-[…]` media variants _before_ the named breakpoints (`sm: md: lg: xl: 2xl:`), so when the same element sets the same CSS property from both and both queries match, the named breakpoint wins on source order. An arbitrary variant that must beat a still-matching named breakpoint therefore has to be marked important — `min-[1440px]:!px-4`, not `min-[1440px]:px-4`. An unprefixed base utility needs no override, because Tailwind already emits it before every variant. `apps/desktop/src/renderer/src/tailwind-variant-order.test.ts` resolves the winning utility through that emission order and fails on a reintroduced plain variant
- one selection rule for every list: selecting a row in Find jobs, Shortlisted, or Applications changes only a background tint and an accent bar drawn inside the row, plus `aria-current`. Padding, margin, and border width are identical in both states and every content line keeps its slot, so a selection can never resize a row or move the rows below it

## Job Finder

Target authority model:

- Job Finder will support `prepare_only`, `confirm_before_submit`, and
  `autonomous_submit` modes under an explicit user-scoped authority envelope.
  Autonomous mode is a core product capability and may complete final submission
  when the saved policy covers the exact application and action. Unknown facts,
  security challenges, unavailable credentials, and actions outside the policy
  still pause rather than being guessed. See ADR 0012.
- The checkout described below is still the prepare-only implementation baseline.
  No existing flag, IPC call, or legacy mode is reinterpreted as submit authority.
  The browser/application architecture is under review and may be replaced before
  autonomous execution is implemented.

Current baseline:

- Users configure which job sources run; importing or suggesting a source does not enable it. Discovery, source-debug, and apply preparation improve through source-generic browser policy, typed evidence, and learned target instructions—not per-board adapters or rescue logic in shared product flow. Reusable provider adapters (for example public Ashby or Workday endpoints) normalize payloads only; see [ARCHITECTURE.md](ARCHITECTURE.md) and [ADR 0007](adr/0007-source-generic-browser-workflows.md).
- The normal user flow is numbered and sequential: (1) Profile, including first-use setup and resume evidence review; (2) Find jobs, which searches enabled sources using the saved profile and search settings; (3) Shortlisted, which reviews selected jobs and prepares optional tailored drafts; and (4) Applications, which records and prepares each application without final submission. Needs you, Tasks, and Settings support this flow but are not extra steps in it.
- Job Finder navigation is the journey and nothing else: Home, Profile, Find jobs, Shortlisted, and Applications. Everything that is reference or configuration — Search plans, Outcomes, Resume approaches, Companies, Safeguards, Documents, and Settings — is grouped under `Everything else`. At CSS widths of at least 1440px the persistent 17rem sidebar lists all of it inline — the journey first, then `Your data` (Documents, Companies, Outcomes) and `Setup and safety` (Search plans, Resume approaches, Safeguards, Settings), then one trailing `Keyboard shortcuts` entry — because a column that is already on screen has the room, and a dropdown inside it only added a click. The collapsed 4rem rail shows the same rows as icons with their tooltips. The sidebar owns its own vertical scrolling with the collapse toggle pinned above it, so a short window scrolls the destination list instead of clipping it. Below 1440px, where the top navigation genuinely has no room, the same reference and configuration destinations live behind one compact `More` menu that carries the rolled-up attention count of what it hides. `Needs you` and `Tasks` stay in the header attention group, and `Tasks` carries that one name at every width. The compact `More` menu carries destinations only, in those same two groups plus the same trailing `Keyboard shortcuts` entry; it holds no introductory prose and no embedded shortcut table, so its seven destinations fit without scrolling at normal window heights, and its internal scroll region and upward flip are the short-window fallback rather than the normal case. Sidebar and menu group labels are 11px eyebrows, never heading tags, so they never enter a route's heading outline ahead of its own h1, and every sidebar count — primary or secondary — uses one identical plain right-aligned tabular treatment with no fill; the only badge-styled counts are the header attention pills. The keyboard-shortcut reference is its own dialog, opened with `?` or from that entry, one row per action with its scope sentence and the keycaps that fire under that scope. One alignment rule governs the header at every width: it is three regions — the wordmark on the leading edge, the module switcher centred, and search, `Tasks`, and `Needs you` on the trailing edge. The switcher is the middle track of a grid whose two side tracks are always equal, so it sits on the window centre line rather than beside the wordmark, and it is never absolutely positioned over the native window controls. Each platform's native window-control inset is reserved inside its own side region — the macOS traffic-light reserve is mirrored on both edges of the brand row, and the Windows caption buttons this frameless window paints itself are reserved inside the trailing region — so reserving either one can never shift the centred switcher off that line and the attention group never sits under the caption buttons. Below 1440px the destination navigation card is centred on that same axis instead of beginning on the shell gutter: it still hugs its contents and neither wraps nor clips, and where the attention group shares that row its reserve stays one-sided so the card stays on one line at the 1024px minimum window and at native 125% zoom. The page content still begins on the shell gutter, and the compact navigation, the compact `More` trigger, and `Needs you` all mark the current destination with the sidebar's own filled surface, indicator bar, and bolder label rather than colour alone. Every route's page title sits at the same offset from the shell: routes add no top padding of their own above the page header, and the shell (or the locked-route layout, for routes that own their top edge) supplies it. The Settings section that governs application sites is named `What Job Finder may do on application sites` and remains a Settings section rather than a destination — a label change only, with every permission, the prepare-only boundary, and the revoke surface unchanged — and Safeguards states the whole application boundary in one sentence and keeps every active application permission inspectable and revocable there. At CSS widths of at least 1440px the brand row spans the sidebar and content columns as one grid row that cannot wrap, so the centred module switcher stays on the header line — the same arrangement the compact layout uses — instead of wrapping out of the 3.5rem header and colliding with the page title; module labels never break mid-phrase. The compact `More` surface is bounded and so is its destination list: the list scrolls inside its own region with edge hints, so at short window heights it can never paint across the pinned `Keyboard shortcuts` footer, and every destination stays reachable by scroll and by arrow keys. In the shortcuts dialog a modifier and its key are separate keycaps joined by a muted, decorative `+` (⌘ + K); a literal-key shortcut such as `/` or `?` stays one keycap
- Every popover, menu, dropdown, and floating panel is placed by one shared rule rather than anchoring below its trigger unconditionally: it flips above the trigger when the space below cannot hold it, shifts back inside the window instead of clipping horizontally, derives its height from the space actually available rather than a fixed value, and exposes whatever does not fit through an internal scroll region whose `more above` / `more below` hints print at the edge they describe. No floating surface has content reachable only by resizing the window.
- Guided setup is five form-first steps: a compact horizontal stepper (Import, Basics, Work history, Job targets, Extras) sits under the page title, and the step's fields start in the first viewport. Extras is explicitly optional and skippable — it merges the former "Your story" and "Screener answers" steps — and the former "Ready check" step is gone: "Finish setup and find jobs" becomes the primary action on Job targets as soon as the required items are met, and stays available on Extras. Setup has one readiness system: the stepper chip carries each step's outstanding count, the step body lists those items, and the sticky footer states in words whether setup can finish and what is still needed. Continue buttons name the next step ("Save and continue to Work history"); only required setup items (a target role, a work mode, one enabled source) block finishing, and optional imported suggestions never do. Finishing lands on Find jobs. Guided setup Basics and Profile › Basics render one shared field list — same fields, same order, same labels — with the location shown on resumes derived from city, region, and country rather than typed into a fourth field. The footer's action cluster is always the rightmost item on the footer's first row at every step, and while required items are outstanding `Save and continue` is a secondary action rather than the loudest control on the step. The stepper reports imported suggestions as "n suggested" rather than "n to review", so only work that actually gates finishing is described as outstanding. Success feedback is one auto-dismissing toast; errors stay on the route. A workspace that has not started setup opens guided setup directly instead of Home, and setup carries its own "Back to Home" control.
- Resume import recognizes every common bullet glyph (● • ▪ ◦ ‣ – — \* -), including glyphs joined mid-line, so role bullets become achievements instead of one paragraph; target roles are seeded from the headline plus recent titles. A multi-stage import that finalizes against a moved profile revision re-reconciles once and stays `applied`/`review_ready`; `failed` is reserved for extraction failures, and the Profile resume strip derives label and tone from the same source.
- Find jobs is the results page. The search settings are a bar above the results — chips for search targets, places, and enabled sources, a small browser-status link, and one `Search now` at its right end — and a chip opens the same editor in place without a route change or a tab strip. A workspace with no results opens that editor; the first saved results close it once. Find jobs never gates Search on the dedicated browser being open: the discovery run opens or attaches it. Only an offline catalog runtime or a genuinely blocked session blocks, and the visible CTA matches that blocker. Result rows show a recommendation badge only when it differs from the default review state and name the source once. The headline numbers describe the jobs worth opening: the sidebar badge and the results header count the leading band only, while weaker matches and clear mismatches are one "also found" pool behind a single toggle whose accessible name is exactly its visible label. Band headings lead with the pool name and separate its part with a middle dot (`Also found · Clear mismatches (2)`, `Title matches · not yet checked (13)`), and a heading counts its whole band rather than the slice on the current page, so it equals the toggle’s count on every page. A remote listing is not penalized against a preferred city when remote is a preferred work mode. The job inspector shows the job before the reasoning — listing text, salary or "not stated", location and work mode, then one "why it fits" line — with the fit breakdown behind a `How this was scored` disclosure; a dimension that knows nothing is dropped rather than printed as an UNKNOWN card. At desktop widths the results list and the inspector each own their own scrolling instead of growing the page. Below that breakpoint the list has no scroller of its own, so its scroll-bottom padding is not applied and a short result list leaves no empty strip under the last row.
- the Find jobs inspector never instructs the user to open a listing the app cannot open. Job Finder has no capability to open an external URL, so when the listing text was not captured the inspector says so and asks the user to copy the link and open it in their own browser; `Copy listing link` stays visible even while the `Not interested` form is open. Every saved search criterion — roles, locations, work modes, sources — carries its own labelled edit control rather than only the empty ones, and a search chip reports only its own section as expanded. While a run is going, the elapsed seconds and the live kept-count appear beside the `Search now` control where the search was started, not only inside the separate activity panel.
- Shortlisted is one column per job, in the order the work happens: the current state and one natural-width primary action, then the resume once one exists, then the job facts with the fit breakdown behind a `How this was scored` disclosure. There is no tab strip and no empty resume tab. Before a resume is ready the destination, sign-in, site-write, and final-submit facts sit inside a collapsed "What happens when you prepare" disclosure, and the daily application count is plain language below the actions. The shortlist column and the job column each own their own scrolling, so reading a long resume never scrolls away the job title or its state. A one-job list carries no search field, density switch, or batch actions, and creating a reusable resume approach is reached from the sidebar's `Setup and safety` group (the compact `More` menu below 1440px) rather than from the middle of preparing one job.
- Tailored drafts carry structured generation provenance (`ai`, or deterministic with a reason: no provider, provider failed, provider timeout, output unverified, forced). Resume Studio states that reason scoped to the _first_ draft ("The first draft came from the built-in generator because…"), and the reason and its detail survive every later save, patch, and export instead of degrading to a generic line. Draft origin and accepted assistant edits are one statement rather than two stacked notes that read as a contradiction: the first-draft sentence comes first, then "N assistant edits have been applied since, and the changed lines are marked in the preview", then the review reminder, with the Undo that restores the pre-edit wording on the same row. When the model wrote the first draft, only the accepted-edit sentence is shown. Deterministic compaction keeps whole sentences, drops truncated duplicates, splits legacy inline-bullet summaries into bullets at render time, keeps more job-relevant grounded skills visible, gives the most recent detailed role the widest budget of its own saved achievements, and repairs dotted technology-token spacing ("ASP .NET Core" -> "ASP.NET Core") without changing any claim.
- the separate browser window Job Finder drives has one name in every user-facing string, `the Job Finder browser`, and three actions with one name each: `Open the Job Finder browser` focuses that window on the paused application, `Reopen the Job Finder browser` is the same control demoted to secondary once the hand-off has happened, and `Check whether this step is done` confirms the user finished the step and came back. The confirm action never renames itself; a running check is a pending state on the same control and a failed check is reported beside the row rather than by renaming the button. Pressing the primary hand-off action produces a visible state change: the open action drops to secondary and the confirm action is promoted to primary in its place. Applications owns the confirm action and `Needs you` uses the same verb.
- the gate shown before Job Finder opens a browser at an employer's application page is framed around that commitment rather than around a sub-option: it leads with the exact job and employer, states that Job Finder will fill the application with the approved resume and stop before the employer's submit control, offers optional screenshot checkpoints as an unchecked checkbox rather than a second confirm button, and carries an explicit `Cancel` alongside the header close control, with Escape and the scrim also cancelling. Confirming without touching the checkbox proceeds with screenshots off, so no accidental activation can enable screenshot sharing, and nothing in the dialog submits an application.
- every application record's detail pane leads, uncollapsed, with the plain answer to "did I apply?": `Not submitted.` plus one sentence saying Job Finder prepared the application and attached the approved resume, and that reviewing and sending it stays the user's. Only a submission the user recorded themselves reads as submitted, and it says so explicitly.
- Applications keeps one box metric for every control in its recovery row, renders the Next step instruction at body size and prints it once, and moves convenience destinations below the recovery actions while an application is waiting on the user so the action row is inside the first viewport at the minimum supported size. A check reports itself in place beside the control that started it instead of through a top-of-screen banner; the latest automatic run appears only while it still needs attention; and the stacked disclosures are named for what they open (`Run details and history`, `More about this application`, `N earlier preparation runs`), each painting a rotating chevron and an underline so the affordance never depends on colour alone. The tracker's table/board/calendar switcher appears only with two or more records, and tracker settings save through the same control and the same four states as every other settings section. The Applications list and tracker panel titles render at the published heading scale rather than as bold uppercase primary-coloured type heavier than the page's own title, the detail pane uses one eyebrow label size rather than two unrelated ones, and the empty state's `Open Shortlisted` and `Find jobs` actions sit inside the empty-state box instead of stranded beneath it.
- Applications distinguishes an autosave pause ("The job site tried to save a field automatically…") from real field conflicts. The Next step callout owns that whole sentence, so the fact strip no longer relabels its two halves. "Finish in the open browser" opens the managed browser page and confirms it inline, and "I finished this step" beside it runs the exact same verification Needs you runs, so the hand-off loop starts and ends on one page. Run/checkpoint/artifact diagnostics collapse under "Technical details" and show only counts that stand for something the user can open. Home counts a source whose latest run completed as healthy.
- First-use Profile setup offers Light edit, Balanced rewrite, and Strong rewrite as tailoring defaults. Strong rewrite can substantially rewrite, combine, or elaborate only supported experience; every generated line remains reviewable, and Job Finder does not invent facts or numbers, auto-approve, or submit applications.

- resume import and profile editing with guided setup and the Assistant; each new import saves a private working copy and records the SHA-256 of those saved bytes. The first-run privacy explanation distinguishes the local working copy from optional configured-model analysis, and setup never implies that model processing is purely local. Import reports all four of its stages every time, with a determinate `Step n of 4`, a progress track, and the longest stage's typical cost stated from the moment it starts; a full import still runs in the foreground and still takes roughly half a minute, and it says so rather than appearing stalled. An import no longer freezes the guided-setup step editor: the user keeps typing into their own draft while it finishes and only saving is paused, with the footer stating the reason, so a late import response still cannot overwrite a concurrent draft. Existing legacy source references are migrated into app-owned storage when their bytes remain trustworthy; missing, unreadable, or changed legacy files show a replace-resume recovery path without discarding extracted profile details and cannot appear application-ready. Extraction retains every grounded role and bullet, derives work mode only from candidate evidence, keeps skills role-specific, and supplements sparse provider output with deterministic grounded evidence instead of silently dropping supported history. Re-importing unchanged app-owned bytes can reuse local analysis only when parser, provider/model, prompt, schema, reconciliation policy, and candidate-context fingerprints all match; refresh actions intentionally recompute because they do not reread and re-hash source bytes. In the profile editor, a half-filled project, language, proof, or custom-answer row blocks saving with a message that names the row instead of being dropped silently, fully empty rows stay optional, duplicate collapse applies only to import candidates (manual duplicates are never removed), the Main skills field stays independently authoritative while full-skill consumers derive their own unions from Main plus skill groups, and role-family fields accept comma- or newline-separated tokens. One router-level guard asks a single composed confirmation before any in-app navigation (link, push, or back/forward) that would lose unsaved profile, setup, or resume edits or abandon an in-progress or failed save; the confirmation is an app-owned accessible dialog that names each unsaved scope and offers Stay on this page or Leave without saving instead of a native browser prompt; hash-only fragment jumps stay exempt. Closing or quitting the whole window with unsaved work asks one app-owned confirmation instead of a native prompt, and an approved discard covers exactly that close — any later unsaved change asks again. When profile content changes in the background while the editor holds unsaved edits, untouched fields adopt the saved changes while edited fields keep the entered values under a notice to review the merged fields; if a safe merge is impossible, an explicit conflict keeps the unsaved edits and requires an explicit save or reload before continuing. A text stage that loses its configured-model call still finishes on the built-in deterministic reader, and Job Finder now says so instead of reporting a clean model run: the stage records why it degraded (a timeout or a provider error, with the provider's own summary), and the run and the profile carry one plain-language warning naming the affected part of the resume — for example that it could not use the AI model for the work history because the model did not answer in time, that it filled that part with its built-in text reader, and that those details are worth checking or the file worth importing again. That sentence appears with the import status it qualifies, in ordinary sentence case rather than as uppercase mono log output: Profile's resume panel prints it beside the imported file and its status, and guided setup's Import step prints it under `Import status` together with a countable hint derived from the run's own recorded stages (`2 of 3 AI stages used the built-in reader`), so a degraded import no longer reads as a clean one behind the word `Ready`. The raw provider detail stays in the run's warnings for diagnosis rather than in the user's notes. A timed-out call counts: it is the most common way this degrades. The degraded stage still produces a usable profile, and no claim in a generated resume gains support from it
- compensation keeps two deliberate truths: the typed search range guides matching and may contain only a minimum worth considering, while the reusable expected-salary answer is separate application text that can name a higher preferred range. Matching normalizes saved and listed amounts to an annual equivalent when their explicit currencies match, so monthly, hourly, and yearly wording are comparable without constraining how a job advertises pay. Unknown or different currencies remain neutral because the product does not guess exchange rates. Leaving the search maximum blank never caps or hides better-paying work, and application use still pauses for review. Deterministic Profile Copilot salary commands respect the same truths: they stage typed compensation operations for exactly the bounds the user expressed, keep stated currency (£/€/$/ISO) and pay period verbatim, inherit only an explicitly saved currency, ask a narrow question with no patch when low amounts lack a period ("2k") or when bounds contradict saved state instead of silently clearing or reinterpreting the other bound, route `salary expectations answer` phrasing to the stored application answer rather than structured compensation, and never convert between currencies or periods
- The profile chat is called the `Assistant`, matching Resume Studio, and is a floating panel rather than a docked rail: it keeps Profile's own geometry, tabs, and scroll ownership unchanged when it opens, keeps clearance from the save footer and the section tabs, and can be minimized to reveal anything it temporarily covers. It uses one schema-validated, review-first operation channel for all supported profile and search-preference edits, including scalar and list fields, structured records, removals, and review-item resolution. The runtime, not the model, owns proposal IDs, timestamps, and review state; chat cannot mark its own proposal applied. Explicit multi-field commands retain each field's own value, explicit `null` or empty-list clears are valid edits, and ambiguous clauses are omitted or clarified instead of borrowing a neighboring value. Apply, Reject, and revision-backed Undo remain separate user actions. The Assistant does not expose application submission or other external workflow authority.
- Profile owns its Save action the way guided setup does: the save footer is pinned chrome outside the scrolling content, so `Save changes`, the unsaved-changes statement, and the background-merge notice that the save resolves are painted inside the window at every supported size. Section tabs never truncate their own name; a finished section shows a check whose meaning is available to assistive technology, and only a section that still needs work spells out what it needs. The `Experience` tab is named `Work history`, matching guided setup and the card inside it, and `Proof bank` / `Add proof` are `Saved evidence` / `Add evidence`. A source's `Include in search` control is a plain checkbox that states On or Off rather than a pill shaped like the `Edit source` button beside it, and optional sections are opened by a control that says `Show` or `Hide`. Profile and every guided-setup step open with Save disabled and `No unsaved changes.`: the derived `currentLocation` line is no longer recomposed from its city/region/country parts while those parts are untouched, so an imported location carrying detail the parts do not can no longer make a profile permanently differ from its own saved record; editing any of the three parts still rebuilds the line, and clearing all three still clears it. The Basics helper text quotes that same stored line rather than a recomposed one, so it cannot announce a value the save would not write. The Profile footer, the guided-setup step footer, and the Settings save bar carry the save state as a dot plus colour as well as words, and a disabled Save is described by that visible line so a greyed control always has an attached, readable reason. The five section tabs sit on one row from 640px up instead of stacking into three rows below 1280px, which is what used to push a newly selected section's content below the fold at 1200x640 and 1024x720.
- browser-driven job discovery across configured targets; setup and Find jobs share one readiness rule requiring at least one enabled valid public HTTP(S) source. The full Profile has a dedicated Job sources tab for large libraries: it summarizes enabled/disabled/attention state, searches and filters the complete catalog, renders only 25 compact rows per page, and mounts the detailed check/guidance editor for one opened source at a time. Importing, suggesting, or storing a source does not enable it; the user must explicitly opt in, and only enabled sources run. Normal discovery targets up to 100 useful candidates across configured sources, while an explicit single-source run targets up to 50 and uses bounded result-pane scrolling and pagination before slower selective review; captured result cards prefer scoped title, company, and location fields so high-volume discovery does not trade away basic listing identity. Explicit target roles remain editable guidance, but are optional: when absent, discovery derives an inspectable search scope from saved target roles, experience titles, headline, skills, preferences, and exclusions instead of blocking on a vague title. Disabled search controls explain and link directly to the missing source owner. Guided setup includes the same basic source name, URL, and enabled-state editor used by the full Profile flow, and cannot complete while no runnable source exists. Independent public provider inventories begin concurrently, fast public-provider source checks avoid unnecessary browser work, and title/location/work-mode triage plus distinct-role budgets rank useful options against the active resume and explicit or profile-inferred scope. Public Lever boards use the matching US or EU provider endpoint from their hostname. Compact collision-safe known-job aliases, versioned fingerprints, freshness deadlines, and provider update timestamps skip unchanged enriched work early while changed/reactivated listings refresh; one shared identity resolver prioritizes provider/source-scoped posting IDs and normalized employer application URLs, treats exact title + company + location + absolute date only as a possible corroborating alias, and refuses ambiguous or conflicting legacy aliases instead of silently selecting or deleting an unrelated posting; later sources budget from distinct newly retained identities so duplicate-heavy early sources cannot consume their exploration share. Every source result explicitly reports inventory completeness as complete, partial, or unknown. Only a complete source inventory can mark a previously known exact listing inactive; partial or unknown observations preserve uncertainty. Interrupted or all-source failed discovery becomes an explicit failed record while safe results and history remain intact; partial multi-source success retains completed work and identifies failed sources for exact retry
- a fit percentage is a headline only when the evidence behind it is more than the listing title. A score earns its headline when at least one requirement was extracted, or role suitability settled to an exact match or a conflict, or preference alignment settled to aligned/mixed/conflict, or compensation was compared against a saved minimum, or evidence coverage reached moderate or high. When none of those holds — a card-only listing whose text was never captured — the inspector reads `Title match only` plus one sentence naming what is missing and what would change it, while a row inside that band carries the same verdict and sentence to assistive technology only — the band's own heading is the visible carrier, so the fact is stated once per band rather than repeated on every row. The number is neither discarded nor used to silently down-rank the listing: it moves inside `How this was scored`, beside the evidence it came from, as `Title-only estimate: n%`. An assessment not bound to the current profile and posting (a legacy row or an offline catalog seed) is stricter still — it reads `Fit not assessed` and carries no number anywhere. One helper is the single authority for that decision, so the row, the inspector, and the breakdown cannot disagree. Preference alignment counts as evidence only when it settled to `aligned` or `conflict`; a `mixed` verdict no longer earns a headline on its own. A requirement counts only when it was actually decided — supported, partial, missing, or conflict — and is not a saved-preference location or work-mode comparison, because the app emits those rows from the saved preference rather than from anything the listing said. Absence phrasing a board writes into a field (`not stated`, `not listed`, `not specified`, `not provided`, `not disclosed`, `unknown`, `n/a`, a dash, or empty — matched source-generically, with no board names) is unknown rather than a place: it can never produce a location conflict the app never observed, never becomes a requirement label, and never poses as captured listing evidence. The saved side follows the same rule: a preferred location that is only absence phrasing — an imported `Location not stated`, or a typed `N/A` — states no geographic constraint, so it is neither tokenised into a conflict against a real listing nor counted as a place the user chose and the listing met. A saved list holding both a placeholder and a real place is judged on the real place alone.
- career stage is a matching dimension. An opening explicitly reserved for entrants — early-careers, graduate, new-grad, internship, apprenticeship, entry-level, campus-hiring, or "class of 20XX" — is a clear mismatch against a profile whose saved evidence is senior, even when role family, skills, and every saved preference line up. It follows the existing conservative-unknowns discipline: the listing's stage is read only from the listing's own words, with explicit experienced wording overriding incidental programme vocabulary; a dated future start is a stage signal only alongside a study-status requirement; the candidate's stage is read from the saved headline, current role title, target roles, and recorded years of experience; and when either side is unknown nothing is emitted at all. The result is a required `seniority` requirement in conflict, which caps the score at the existing hard-conflict ceiling and produces a `skip` recommendation whose reason is printed on the row, quoting the listing sentence it read the stage from and the profile fact it was compared against.
- Find jobs, Home, and Search history share one search vocabulary from a single helper, under the invariant that the kept count equals the jobs worth opening plus the jobs also found. Find jobs' banded headline carries the reconciling total as its accessible and hover text, and no surface reports a reviewed-listing volume — the raw number of listings a run looked at — as if it were a result count. The two populations have two different words, so one screen can never appear to contradict itself: `saved` is everything a run added to this device and `kept` means one thing everywhere — the jobs the current search plan holds. Home reads that plan population before it prints anything, so it either agrees with Find jobs (`Your last search: 15 new jobs saved · 35 duplicates merged.`) or states both populations plainly when a run saved more to the device than the active plan retains — on the finished-search status line (`Search finished · 50 new jobs saved on this device · 15 kept in your current search plan.`) and, only when no status line is showing, in the recommendation body (`Your last search: 50 new jobs saved on this device · 15 kept in your current search plan.`), so the clause is printed once rather than twice in the same card. `kept` is the plan's ids intersected with the jobs Find jobs actually lists, never the plan's own unpruned id ledger, so dismissing or applying to a job cannot drift Home away from Find jobs, and Find jobs' reconciling line says the total belongs to the plan (`15 jobs kept in this search plan.`) rather than to one run.
- an evidence-backed fit ledger that separates required, preferred, and inferred requirements; cites exact listing text and resume/profile sources; marks support, missing evidence, uncertainty, and conflicts; and keeps both the numeric score and conservative strong-fit/original-CV/review/skip recommendation grounded in that evidence. Scorer/session version 4 adds typed role suitability, preference alignment, application effort, evidence coverage, and compensation; orders conservative recommendation before aggregate score; rejects non-openings and unrelated role families; and treats geography, clearance, and thin required evidence conservatively. Unknown evidence stays neutral: a listing that names no concrete work mode beyond flexibility stays unknown rather than compatible in preference alignment, and unpreferred concrete modes listed alongside flexibility stay ambiguous instead of becoming conflicts; Easy Apply changes only effort, hard conflicts stay hidden by default, and the full requirement ledger remains inspectable. When a saved result is meaningfully reassessed or changes position in the visible ordering, Job Details preserves the latest change and explains it through a closed `Why this result changed` disclosure; unchanged refreshes do not erase that explanation. The shell reflows all six Job Finder destinations at practical native Electron 125% zoom at a normal desktop window — the current low-vision acceptance point after native 200% zoom was removed from current acceptance by user decision because it collapses into a mobile-like layout with low diagnostic value — while recommendation badges wrap inside their parent assessment cards
- workspace opening uses the same final header, responsive navigation rows, persisted sidebar width, content origin, and route heading as the loaded shell, with quiet in-place placeholders instead of a centered card that reflows the whole app. Later route-chunk loading keeps that shell fixed and centers its status card in the remaining workspace. macOS reserves the native traffic-light area before the wordmark in normal and maximized windows and mirrors that reserve on the trailing edge so the centred module switcher stays centred, then reclaims both in native fullscreen; Windows keeps app-owned minimize/maximize/close controls available during loading and after load and reserves their exact width in the header's trailing region, and Linux retains its native framed controls without a duplicate renderer set
- source-debug that learns reusable target instructions from schema-safe DOM/text evidence and bounded visual evidence
- resume workspace with ATS-first preview, export, approval, template selection, manual experience ordering, explicit editor representation for every canonical work-history record, and an atomic pre-mutation version ledger capped at the newest 100 persisted revisions per draft; exact review-required restore survives restart, clears approval/export readiness, protects newer edits from stale saves or slow AI regeneration, and marks the restored draft as needing a fresh review before it can export again rather than presenting it as failed generation. Requested section rewrites are review-first like Guided Edits: regenerating a section produces a pending proposal bound to the current saved draft and changes nothing until the user explicitly accepts it, unusable model output yields a truthful no-change message instead of a fabricated patch, and model-proposed lines always carry assistant provenance so generated wording never presents as user-authored. New drafts use the exact target role as the headline, place supported job skills first, rank accomplishments and projects for the vacancy, suppress repeated metrics, and hide weak or unrelated history by default in balanced/aggressive modes while keeping every canonical role available in the editor. Conservative mode preserves compact career continuity. Aggressive mode substantially rewrites, combines, and elaborates cited evidence: it may spell out plausible engineering details around an evidenced stack (JavaScript, TypeScript, Next.js), product domain (restaurant management SaaS), and existing metrics, and experience/project bullets may cite profile-level skills as supporting evidence. It also carries two explicit, user-owned relaxations, each only on lines the model itself marks inferred: evidenced years of experience may be rounded up by at most one year when the job listing itself states that rounded years figure, and a line may name any technology the target job listing itself asks for when the candidate's saved evidence shows professional technical experience — never a technology absent from both the evidence and the listing. In aggressive mode the job's requested technologies are also added to the skills section, named in a note for the candidate to confirm. The purpose is stated plainly in the product copy rather than euphemized: these are small, deliberate claims designed to clear the job's screening and earn the first interview, kept bounded to what the candidate's evidence implies they can actually do; proving them happens in the interview, and only the candidate can do that. The whole draft remains review-required, and hard facts such as employers, dates, titles, credentials, seniority, leadership, and any number other than the rounded-up years figure still require saved evidence; model-flagged inferred lines are counted in the draft notes with the explicit warning that confirming these lines — which go beyond the saved evidence — is the user's own responsibility, and every relaxed line stays export-blocked until the user confirms it is accurate and their own. Experienced profiles target two pages rather than forcing a crowded one-page document. All eight apply-safe templates use flat recruiter-grade typography, restrained rules and accents, and print-aware entry pagination rather than card-like document chrome; rendered HTML or PDF exports record the SHA-256 of the exact output bytes and preserve their exact format
- a persisted application-CV choice: Settings defines the default for jobs shortlisted later, while every shortlisted job keeps its own `Original CV unchanged` / `Tailor for this job` selection; changing one job never changes another current job or the Settings default, and Apply Copilot remains an explicit per-job action in both modes
- Shortlisted supports bounded sequential preparation of the next up to 10 eligible tailored drafts. A curated batch selection persists per active campaign across navigation, and staging the batch consumes that curated selection so returning to Shortlisted always shows the true pending set. Each draft still requires individual review and approval; this preparation never exports, approves, queues, submits, or sends drafts automatically.
- eight apply-safe templates: `Chronology Classic`, `Senior Brief`, `Modern Editorial`, `Engineering Spec`, `Proof Portfolio`, `Formal Proof`, `Longform Timeline`, and `Career Pivot Bridge`
- target-generic prepare-only apply flows that fill exact grounded profile fields, attach the explicitly selected original or tailored resume, advance only clearly non-final steps, and stop at final submission or any ambiguous gate. Bounded ATS autosave is default-off and can be selected only through an inspectable one-job policy with one origin, one resume digest, current approved answers, and future expiry; every field rechecks that exact policy revision. It never authorizes or clicks final submit. Every catalog/browser execution path remains `prepare_only`; legacy submit-capable modes pause without a submitted outcome or timestamp. Tailored approval and both original/tailored pre-apply paths reject missing integrity records, missing files, or SHA-256 mismatches before browser work; readiness names the exact CV, redacted destination, unresolved user work, allowed intermediate writes, and disabled final submit; afterward, each new result carries a redacted receipt and can export a local redacted JSON packet without credentials, raw browser state, URL secrets, or local file paths. Local fills are not reported as site writes; only a successful authorized autosave response is. Receipt fields attest to Job Finder's own authority and actions rather than proving the site's outcome
- a unified, persisted `Needs you` Action Inbox with distinct login, signup, MFA, email-verification, CAPTCHA, account-choice, manual-answer, legal-consent, redirect, upload, and other presentations. Every classified application blocker persists. Authentication kinds verify source access; challenge/control kinds verify that the exact blocker is absent. Done schedules at most one run/job/result/checkpoint-scoped `prepare_only` retry: the same blocker stays blocked, no blocker resolves, and a changed blocker becomes a new pending request. Stable identity, claim/CAS/idempotency, restart, and multi-instance protections prevent duplicate execution; no request grants credentials, account creation, intermediate-write consent, or final-submit authority
- `Needs you` counts one population everywhere: unresolved user-action steps. A view that deliberately shows a different, wider population never borrows the name — it states its own population and unit instead, so two numbers on one screen can never both read as "Needs you" and disagree. Applications is the worked example: the row badge marks a step (`Needs you`), while the filter over whole records is `Waiting on you`, whose accessible name carries the unit (`Waiting on you: 2 applications`).
- `Needs you` is visually and semantically separate from the sequential Profile → Find jobs → Shortlisted → Applications workflow, while Settings remains available with the workflow destinations
- every card in a queue called `Needs you` is required, so no card carries a REQUIRED chip; only the exception, an optional step, carries one. `Cancel this step` states its consequence: cancelling closes the step without doing it, Job Finder stops working on it, nothing is sent, and the user can still finish it themselves on the job site.
- A `Needs you` card names the step, links into the exact Applications detail it belongs to, and carries the boundary as one plain sentence ("Confirming here cannot create an account or submit an application.") instead of a boxed footnote in internal wording. Dismissal is one named control, `Cancel this step`; the page subtitle keeps "Passwords and security codes stay with you."
- discovery keeps Current Search in the same first-column position before and after results arrive; established layouts place Job Results in the center and Job Details on the right. The Job inspector always describes a job that is actually visible: when pagination, search, or sorting moves the selected result off the current page, the inspector synchronizes to the top of what is shown (and clears when nothing matches) instead of describing a row the user cannot see. Results sort deterministically by best match, newest listing-origin date, or company with a direction toggle; the choice persists locally and clear mismatches stay at the bottom under every pivot. Result rows and details retain source attribution and support source, workflow-status, listing-activity, work-mode, and fit filters. Listing activity is a derived snapshot fact, separate from workflow status: active comes from the newest verified/seen evidence, inactive from a complete source inventory, and stale/closed from typed listing signals. Inactive and stale are review warnings rather than proof of closure; an exact closed signal blocks shortlist and application preparation in the service as well as the UI. Result counts separate what a run collected from what remains retained and reviewable, and while a search streams in, the header reports what is actually visible after the persisted query and filters ("X of Y matches ready to review") instead of a raw total
- nested pane scrolling on wide locked screens is native first: while the pane under the pointer still has scroll range in the wheel's direction the browser scrolls it, at full delta and with its own momentum, and the app neither cancels the event nor writes a scroll position. The app arbitrates only at a true boundary, where the pointed-at chain is exhausted — an unmarked inner scroller inside a pane (kanban column lists, CV preview wells) is exhausted before its marked pane, text controls always keep browser-owned scrolling, and each owner hands control outward from there. Previously the route header was offered every wheel delta first and was credited with absorbing its own height even on routes whose outer scroller had no scroll range at all, so those pixels were written into a scroller the browser immediately clamped back and were lost. Changing result pages resets only the result list while preserving the rest of the screen, and once scrolling settles the page header snaps fully in or fully out, so no input source can leave the route title clipped through the middle of its glyphs under the fixed shell
- `Not interested` is an explicit local preference, not a ranking fact: users choose one or more bounded reason chips before hiding a result, hidden jobs remain reviewable in a disclosure, and `Show again and reset` restores the job and clears the feedback. Rediscovery preserves the explicit hidden state across stable aliases while accepting refreshed factual listing and fit evidence
- Search History preserves a typed per-run change digest for new, unchanged, changed, reactivated, inactive, known, and skipped listings, plus duration, warnings, and per-source health. The digest prints a card only for a state the run actually saw; when nothing changed it says so in one sentence rather than showing a row of zero-count chips. Failed sources can be retried only through the existing single-source operation; history never turns retry into a broad replay.
- tailored-résumé generation exposes a readable labelled progress bar with its estimate outside the fill and keeps the same displayed progress when the user leaves and returns while that operation is still pending
- Resume Studio opens on a viewport-bounded live preview beside an independently scrollable editor. The document has two states, Draft and Approved; approval clears when the draft changes and the existing re-approval path applies. The primary action is `Approve resume`: unsaved edits are saved first, Job Finder creates and verifies the exact application PDF in the background, and approval binds to that artifact without opening a native Save dialog. Downloading a PDF copy is an optional secondary action. Non-blocking suggestions never create a global review step; only real approval blockers interrupt the primary path. The studio has exactly one AI control, the `Assistant`: per-section rewrite, "Retry with AI", and "Refresh draft" are gone, and a whole-draft rewrite is one Assistant action that states what it replaces and asks once before it runs. Assistant proposals render as inline before/after diffs on the proposal card. Clicking a section or bullet in the preview focuses its editor field and scrolls it into view; the tools pane keeps only the focused section open, and skills the tailored draft dropped are click-to-add chips beside the skills editor rather than an inert list. In the coverage comparison, each line the tailored draft dropped carries its own labelled `Restore this line` control on its own row instead of an inline link that reads as the last word of the sentence. The tools pane leads with actual resume fields; keyword/evidence comparisons and supporting job context come later, with the latter under plain `About this tailored resume` copy. After approval, `Prepare application` is primary and returning to Shortlisted remains available as a single secondary `Back to Shortlisted` that points the same way as the workspace header's own back control; `Save draft` reads `Save draft (re-opens approval)` once a resume is approved. The studio pins no banner stack above its panes: one compact sticky row carries the state badge, the next-step sentence, an optional `N items need attention` chip, and the single primary action, while every notice that row used to pin — blocked claims, hidden work history, blocking validation, set-aside proposals, the focus announcement, the claim-confirmation panel, and the application-PDF status — renders inside the scrollable tools column, which the chip opens. The rendered page holds a readable minimum scale while the column can show the whole page at it. The preview watches a zero-height width probe beside its scroll region — never the scroller itself, whose scrollbars the scaled page changes — and re-fits only when that width actually moves by more than a pixel, with a dead band between the two automatic modes, so the page settles at one scale instead of zooming in and out; opening the Assistant is not such a change at all, because the Assistant is a floating panel and never takes a studio column. A column too narrow for the readable floor gets the whole page fitted to it rather than a document cut off mid-line under a banner that says to approve the resume shown in the preview. A `Fit width` / `Readable size` control still lets the user pin either choice, and the larger reading size scrolls horizontally inside the preview's own region. The studio content area is height-bounded at every supported width, not only at desktop: each pane and tab scrolls inside that bound, so a thread holding a grounded proposal can no longer grow the route tens of thousands of pixels and strand its Accept/Reject controls below the fold. Exactly one row sits above the panes — the studio's own compact state row — and it is the only owner of draft state: the workspace title row above it carries navigation, the job identity and the updated/approved timestamps on one line, and no longer repeats the `Approved` or `Unsaved draft` badge that row already states beside its next step and its action. The workspace title row is ordinary scrolling content that scrolls away like every other locked route's, so at scroll position 0 the studio's bottom edge sits roughly one title-row height below the fold until the user scrolls it off. At compact widths the studio is a two-tab surface, Preview and Tools — the Assistant is not a tab at any width — and the `Resume Studio` block and the `Save draft` / `Download PDF` toolbar live inside the Tools tab's scroll region, where desktop has always kept them, rather than occupying a second permanent band. The `N items need attention` chip counts every notice the panel can open — blocking validation issues, a blocked-claims reason, the work-history warning, and a set-aside proposal note — and when nothing is outstanding the panel stops claiming otherwise and reads `Resume checks`. The tools column always ends above the launcher pill's band rather than only clearing it at the end of its scroll, so a card or disclosure can never pass underneath the pill mid-scroll, and that reservation is constant: expanding the Assistant hides the pill but moves nothing in the column
- The studio Assistant is a solid floating viewport panel at every width, exactly like the Profile Assistant, and it never reflows the studio: it rests over the studio's bottom-right corner, is 384px wide on a full desktop window and 360px below 1280 (never under 320 unless the window itself is narrower), is height-bounded to the viewport so its composer is always visible, supports drag and minimize controls, and badges new replies instead of reopening itself or moving the underlying workspace. It is a fixed, body-level surface: scrolling the studio panes never moves it, and a drag toward the desktop's edge stops at the window's, so no part of it can leave the window at any size. Minimizing folds it into the launcher pill at its own bottom-right corner and reopening expands it back out of that corner, so the thread never appears to jump; the short fold animation is disabled under reduced motion. It has no docked variant and no `Assistant` tab: the studio grid is two columns — preview and tools — whether the Assistant is open, minimized, or closed, and every class on both panes is identical in all three states, so opening it shifts nothing. Crossing the desktop/compact breakpoint therefore changes nothing about the Assistant, and exactly one transcript is mounted at any width. A pending proposal's decision row is sticky at the bottom of the transcript, so it is painted whenever the proposal card is in view; a pending reply shows an elapsed counter beside a stated expected range and escalates below that range rather than above it; and every resolved proposal leaves a persistent result line in the thread, so a click on a decision control can never produce a zero-pixel diff. Approving the resume still sets pending proposals aside, and the note says they remain in the Assistant thread and offers `See the suggestion` beside `Got it`. Each proposed change names its target location and offers a collapsed `Why this edit is grounded` disclosure that shows the nearest linked source excerpts and the saved claim assessment only when it still matches the exact current saved text; missing targets, refs, or validation degrade to explicit not-checked copy, and every disclosure states that proposed wording is checked after the user accepts and saves. While a proposal is pending, the transcript stays pinned to its end through window and panel resizes so the Accept/Reject controls remain fully visible at short desktop heights. Approved résumé state names the exact artifact and exposes one clear continuation back to Shortlisted. There is one Assistant implementation in one placement, not a differently named copy of the thread per width: the same whole-draft rewrite with its confirmation and the same `Reload workspace` recovery on a long-running reply appear at every window size, and the panel never sizes itself — the floating shell's viewport-bounded box owns the bound and the transcript scrolls inside it
- a proposal's grounding verdict is never a bare word. Both outcomes share one heading, `Checked against your saved evidence`, followed by an outcome sentence and the reasons behind it, always rendered together: wording unsupported by saved evidence blocks approval; a deletion of the user's own text is described as a removal, never as grounded; and a proposal that introduces no blocking wording says so. The proposal-level summary states two facts from one evaluation — how many proposed changes would introduce an approval blocker, using the shared export gate's verdict, and how many blockers the saved resume already carries, counted with the exact predicates the export/approval validator uses — so a proposal is never described as making the resume approvable when it does not. That per-change verdict is computed when the proposal is made and is not recomputed if the draft moves on afterwards, so the export/approval validator, not the proposal card, remains the authority at approval time and can still block a change the card cleared. `Accept anyway (N)` is the quietest button variant, states that it keeps approval disabled, and is joined by `Edit this wording myself`, which opens the exact editor field the blocked change lands on without writing the unsupported wording into the draft
- Shortlisted exposes one state-driven primary application action above supporting evidence. Its label, enabled state, blocker, and nearby recovery action derive from the selected job and résumé asset, while uncommon proof and diagnostics remain available below or through disclosure
- a global `Tasks` panel derives truthful stages, source/count progress, supported cancel/restart/review actions, and history-based estimates from persisted run state without a fake countdown
- cancelling application preparation aborts the active browser work instead of only changing the saved label, closes the app-owned working tab, and prevents late browser results from changing a cancelled run back to running. Closing the app waits for active preparation to stop; any still-running record is saved as failed with a clear restart path and an explicit statement that Job Finder performed no final-submit action. The same recovery runs after a hard process interruption so `Tasks` cannot reopen with a permanent ghost task. Recovery also settles every still-planned result of such a run to a terminal failed outcome whose copy says preparation stopped before review and no final submit occurred: genuinely begun results keep their preparation-start marks and keep counting toward the daily begun-application limit, never-begun or legacy results record an explicit no-start instead of legacy uncertainty, results awaiting review or already finished are left unchanged, and rerunning recovery changes nothing further
- a bounded local product-action registry lets an assistant read workflow summaries, inspect Needs you and an exact apply run, create review-only Profile Copilot proposals, shortlist/dismiss/restore one job, or open one existing user-action page. Mutations carry explicit confirmation policy and typed receipts; proposal tools never auto-approve, and no tool can submit, create an account, navigate arbitrarily, read files, or mutate an external application
- meaningful edits expose saving/saved/failed state with stale-completion protection, exact retry, guarded navigation, and restart receipts; high-churn workspace updates can arrive as revisioned slice deltas with stale/gap rejection and one-shot full-snapshot recovery
- résumé imports can propose evidence-cited timeline repairs for overlaps, gaps, ambiguous or reversed dates, split/duplicate records, and missing employer/title; every proposal is independently accepted, rejected, or undone with stale-edit protection
- manual application answers distinguish one-use storage from an explicit future Profile save. Suggestions show provenance, prior question, confidence, scope, and revision; they are never inserted automatically and conflicting saved answers are never overwritten
- Documents is a dedicated secondary destination for resumes, cover letters, portfolios, work samples, transcripts, certificates, and images; Settings links to it instead of embedding the library. Imports are copied into app-owned storage, signature/MIME and size checked, SHA-256 verified, and shown to the renderer as metadata only. Each asset carries sensitivity, retention, and an explicit private-storage, application-attachment, or assistant-context consent scope; importing an asset does not automatically attach it to an application or expose its bytes to a model. `until_deleted` remains the default, while 30- and 90-day policies are explicit choices whose clock starts only after import succeeds. Removal or expiry makes an asset unavailable immediately and moves it to Trash for seven days; restore requires an explicit policy and starts a fresh clock, after which ordinary filesystem deletion purges the stored file and metadata. Supporting-file uploads obtain bytes through a main-process-only loader that rechecks lifecycle, consent, size, and SHA-256 immediately before the browser file control is filled, so later removal or tampering blocks the upload. This is recoverable product deletion, not a forensic secure-erase guarantee
- Applications can create a cover letter or short-response document for one exact application and optional file question. Deterministic generation ranks approved profile summary, proof-bank, experience, project, and skill evidence against the exact job and question, removes repeated evidence, and composes a concise proposal from at most three evidence blocks; the preview exposes every excerpt. File-question generation requires current exact run, result, question, job, and ApplicationRecord lineage and fails closed when that lineage is stale or crossed. A user can edit the proposal and save the text only as a new compare-and-swap revision. Edited revisions are marked user-authored and require claim review because their changes are not automatically grounded. Approval is revision-specific, copies the exact text into Documents & assets with the accurate `cover_letter` or `application_response` type and application-attachment consent, and still requires the user to select and save it for the exact file question. Export is a separate local `.txt` action. None of these actions grants account-creation or final-submit authority
- Settings can export a strictly allowlisted local diagnostic JSON containing build/capability/timing/warning summaries while excluding credentials, raw résumés, transcripts, audio, screenshots, browser storage, and private payloads
- Settings keeps its section-scoped saves, and every section now shows the same four save states while a sticky bar names anything still unsaved from anywhere on the page; with two or more dirty sections that bar names them and offers navigation only, because one button cannot honestly say what it would write. The section nav is real navigation — one `aria-current` item, an active fill plus an indicator bar, hover and focus affordance, and a destructive tint on `Danger zone`. Choosing a theme applies it immediately and `Save appearance` remains the commit. `Workspace behavior` is renamed `Browser & saved jobs`. The save bar is always rendered, matching the Profile footer: when nothing is dirty it says so and offers no button, and when one section is dirty it names that section and offers that section's own save. Every section that commits reports its own confirmation — `Appearance saved`, `Resume preference saved`, `Workspace behavior saved.`, `Tracker settings saved.` — until the next edit, so a committed save no longer looks identical to a section that was never touched. Tracker settings are saveable at all: on a workspace with no persisted tracker record the screen used to parse a fresh default on every render, which re-seeded the editor continuously so the section could never become dirty, publish to the save bar, or be written
- Safeguards leads with `What Job Finder will never do` — the boundary card, the active application permissions, and the revoke control — always visible, with the event engine behind one disclosure that is open by default only when there is an active blocker. The `All` count equals the sum of the categories and the rendered list, no card renders for a threshold that has not been crossed, timestamps are plain dates and times, and every named recovery action has a control that performs it

### Campaign command center and application tracker

- Every workspace has one durable active search campaign. Existing workspaces receive a default campaign without losing their jobs, sources, history, applications, or review work. Precision mode starts with a small high-quality target; Scale mode starts with a much larger retained pool and controlled preparation batches. Both modes remain editable rather than hiding fixed presets.
- A campaign owns its roles, locations, work modes, exclusions, compensation, sources, fit threshold, retained-job target, preparation limits, resume policy, progress, and history. Find jobs, Shortlisted, and Applications show the active campaign. Workspace-wide search can still find an item from another campaign and switches campaign before opening it.
- Job Search Home is the command center. It shows persisted counts, source health, active work, follow-ups, interviews, response rates only when a real denominator exists, and one derived next action. Zero-count status is not shown, and Home reports the same number Find jobs reports, from the shared count helper. `RECOMMENDED NEXT` follows the search loop: with unreviewed jobs it offers reviewing them by name, and `Search again` is available to any returning user with sources. `Pause background work`, the search-plan selector, and the plan-volume line are off the landing surface, and the plan selector appears only with two or more plans; workspace-wide local search and the global browser/application pause remain available.
- Results, Shortlisted, Applications, Needs you, Search History, Documents and assets, campaign lists, and resume version history have local search with result counts and no-match states. High-volume surfaces keep selection and filters stable, support keyboard movement, and use saved density/views or columns where those controls are useful.
- Preparation is the Applications page: one list with a status chip per application and its detail. The local CRM — Discovered through No response stages, table/Kanban/calendar views, a timeline, reminders, interview dates, contacts, notes, Candidate Asset attachments, compensation and offer details, tags, custom stages, duplicate hints, and CSV/JSON export — is a separate `Tracker` destination on the same route, reached by an explicit "Open tracker" link that appears only once at least one application exists. It is never a peer tab beside Preparation. The configurable no-response rule defaults to 14 days. Preparation approval timeline entries describe opening/filling for preparation only, including legacy entries, and never imply submit approval. Externally observed stage changes use a blocking confirmation modal that isolates background focus, traps keyboard traversal, closes on Escape/backdrop cancel, and restores focus to its opener. Marking an application Applied or later is a manual tracking fact only; it never grants browser submission authority or invents a submission receipt.
- The global pause prevents new discovery, source-check, and application work and stops active owned work while local review and editing remain available. Campaign stop rules pause a preparation batch for configured login/signup blockers, changed or unexpected forms, unresolved human-answer or eligibility uncertainty, and an abnormal blocked/failed rate after its minimum sample.

The six phase-two slices below are implemented in the current checkout.

- Campaign rules are enabled/disabled `Must have`, `Prefer`, and `Never` rules over the evidence-backed job fields. Hard exclusions stay separate from ranking preferences; each rule shows its provenance and freshly measured remove/downgrade/unknown counts against the campaign's real retained jobs. The pre-search funnel is derived only from those real jobs — a campaign with no retained jobs yields a zeroed funnel instead of any invented estimate.
- Local schedules are enforced: run-now, daily, and selected-weekday runs respect time-zone-aware start times, pause windows, campaign limits, and the existing safe stop conditions; every finished run updates persisted run facts. A campaign digest persists new, changed, reactivated, inactive, known, skipped, and failed-source outcomes, and local in-app notifications cover strong new matches and blocked/failed campaign work. No external email or push integration exists.
- Outcome analytics record only explicit user-controlled outcomes on applications and slice them by campaign, source, job title, company, and resume strategy with real sample sizes, visible uncertainty, and conservative, inspectable, resettable suggestions that never change objective job facts. Recording rejects ambiguous identity: a job in multiple campaigns requires the campaign identity, and a job with multiple application records requires the application identity; the selected identities are persisted with the outcome. Recording `Applied` remains a local tracking fact and never creates submission evidence.
- Named role-family resume strategies carry a base resume, template, headline/skills/coverage policy, tailoring strength, evidence boundaries, campaign default, and a per-job selection with an inspectable reason. Automatic role-family recommendations and per-job selection are enforced. Strategy resolution passes the selected policy, including the base document and headline, skills, coverage, tailoring, and evidence boundaries, into résumé generation; reuse never approves, readies, or un-stales an artifact. A campaign default remains persisted when its strategy is disabled; the strategy screen keeps that disabled default visibly represented with an explicit clear action, while disabled strategies cannot be newly assigned.
- Search plans are optional reusable discovery settings for separating roles, sources, how much each run searches, rules, and progress; they do not configure or authorize application volume. They are reached from the sidebar's `Setup and safety` group (the compact `More` menu below 1440px), describe their volume in plain language, and keep Delete on its own row away from the safe actions. The current default plan is enough to start finding jobs. Find jobs names the current plan in a `Plan` chip on its search bar and switches plans there as well as on Search plans, because `Search now` always runs the current plan and a search reads its plan, settings, and preferences when it starts, so a saved edit is felt on the next search rather than a run in progress; the plan editor, the search-settings save, Settings, and Safeguards each say so at the moment of saving, and the plan editor offers `Search now with this plan`. Discovery filter, sort, and view selections persist per plan, so switching plans cannot leak selections between plans. Application preparation instead has fixed product safeguards of at most 10 unique employer jobs per run and 20 begun employer applications per local day across campaigns and legacy lineages. The dashboard separates exact usage from uncertain legacy capacity and shows the next local-midnight reset. Resume approaches are optional reusable role-family rules reached from Shortlisted job context: a job uses an explicitly chosen approach, and when the job has no explicit choice its search plan can name one enabled approach as a fallback recommendation; assignment and reuse never approve or make an artifact application-ready. Editing an approach shapes the next tailored draft only; each approach card says how many shortlisted jobs use it, and after an edit the page offers to re-tailor those jobs' unapproved drafts while approved resumes are left untouched.
- Company intelligence pages keep the complete company-linked saved-job collection, including openings hidden from the active discovery result set, plus applications, outcomes, contacts, notes, salary/offer evidence, source history, and preferences. Exact canonical names and user-approved merge aliases can own jobs; legacy aliases and domains cannot. Domains remain corroborating/conflicting research evidence, and existing jobs are not destructively rewritten around domain guesses. Listing activity stays distinct from application workflow state. A stale company URL shows `Company not found`, selects no substitute, and offers a route back to Companies. Conservative duplicate handling surfaces near matches as explicit merge-review candidates that require a user decision.
- High-volume safeguards add per-company/time-window application caps, simultaneous-application conflict checks, stale/closed/suspicious listing signals, abnormal-failure pauses, quality-review sampling, contradictory-answer protection, reversible dismissals, and explicit recovery guidance — all local tracking facts that never grant submission, credential, or account authority. Technical discovery/source-debug and application-terminal failures create idempotent safeguard and in-app notification evidence, while explicit user-owned blockers such as login, consent, CAPTCHA, MFA, or manual steps are excluded from the technical-failure sample. Contradictory grouped answers persist idempotent advisory safeguard evidence even when a reusable decision is rejected. Analysis concurrency remains saved policy rather than a runtime cap.
- `Not interested` can optionally hide the exact job and add an exact-normalized-employer exclusion in one atomic mutation after a preview. The reusable exclusion is offered only when company identity is unambiguous; domain evidence may corroborate identity but never creates a domain-wide exclusion. Reversal removes only the referenced employer exclusion and does not silently restore or rewrite unrelated job feedback.
- Application records are exact application lineages, not one mutable row inferred from a job. Starting another application creates a separate record; apply results, attempts, questions, answers, artifacts, replay checkpoints, consent, Needs you requests, campaign projections, and grouped-answer actions stay bound to that record. Manual answers preserve their exact run/job/result/question/application lineage and provenance; reusable Profile saving remains a separate explicit choice.
- CRM single-record and bulk stage changes merge against transaction-current application records. Bulk changes carry every visible expected revision, commit all selected records or none, retain manual tracker provenance, require confirmation before claiming externally observed stages, and keep failed selections available for explicit retry with a visible error. Company salary evidence must name its exact current job; offer evidence must also name the exact ApplicationRecord. Failed evidence saves retain the user's draft for correction instead of clearing it.

Current implementation rule: live submit remains disabled while the new authority
architecture is unimplemented. Current application runs keep
`submitAuthorized: false` and `accountCreationAuthorized: false`; the final
application control is never clicked. This is a migration state, not the target
product ceiling. See [ADR 0012](adr/0012-user-scoped-autonomous-application-authority.md).

## Interview Helper

Current baseline:

- target-context setup from Job Finder records or direct user input
- rehearsal checks for transcript language, runnable audio/transcription paths, providers, screenshots, assistant responses, and retention defaults
- capture consent is capability-specific: a user can run system-audio-only, microphone-only, or screenshot-free sessions after accepting the core transmission, retention, and visible-surface disclosures
- a visible main-window conversation with explicit text sends, temporary PNG/JPEG/WebP attachments, screen-capture context, assistant replies, and a recent source-labeled transcript
- user-started microphone and meeting/system-audio transcription with queued transient chunks, local-command STT or configured cloud fallback, native-caption intake, and cue generation behind typed provider roles
- live sessions start with an empty transcript and no synthetic cue; standalone Whisper non-speech markers and short recorder-tail fragments are discarded instead of becoming coaching context
- post-session review, export, transcript annotations, delete, and explicit Job Finder follow-up actions
- starting an interview opens visible answer and transcript popup windows by default while the main conversation remains available; the answer popup is a compact interactive assistant with typed sends, pasted or selected image attachments, explicit screen-context capture, answer copy, and hide/reopen controls, while the transcript popup stays optimized for live source text with copy and hide/reopen controls
- popup workspace updates are event-driven rather than renderer reloads so live audio/transcript changes cannot erase an in-progress answer draft or pending image attachment; popup position and size persist across sessions
- personal-story requests without candidate evidence produce a bracketed STAR scaffold instead of invented employers, incidents, technology, timelines, metrics, or outcomes; screenshot-dependent questions use model-backed visual observations when available and otherwise disclose that visual analysis is unavailable instead of guessing from earlier transcript context

- active sessions expose a derived Session health summary for microphone/system-audio signal, transcription backlog and fallback, cue latency and fallback, popup visibility, and the latest recoverable failure. It uses existing local session state, preserves the session on failure, and never writes back to Job Finder automatically\r\n\r\nHard rule: capture, screenshots, attachments, advanced overlays, and model use must stay explicit, visible, auditable, and adapter-owned. See [ADR 0003](adr/0003-interview-helper-live-session-architecture.md) and [ADR 0008](adr/0008-visible-first-interview-helper.md).

## Product Defaults

- desktop shell: `Electron`
- renderer: `React`
- local persistence: `SQLite`; desktop startup enables rotating database-only recovery snapshots on graceful close plus a separate pre-reset snapshot. These snapshots do not include generated resumes, Candidate Assets, application documents, or browser-profile files
- browser runtime: managed sessions first
- AI: provider abstraction with separate chat, vision, STT, and embedding roles
- configured generative routing uses OpenCode Go: `muse-spark-1.3-contributor` through Responses with requested `xhigh` reasoning for text, tool, and image work, and `deepseek-v4.1-flash` through Chat Completions with `high` reasoning for aggressive resume tailoring only. Audio transcription remains local Whisper or an explicit audio-capable model. See ADR 0019 for routing and ADR 0009 for the contract-first agent boundary.
