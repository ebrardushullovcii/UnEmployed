# Design References

Use this directory as visual reference only.

## Rules

- screenshots are the primary reference
- prototype `mockup.html` files are disposable inspection artifacts, not production code
- design references do not override product scope, contracts, or active plans
- when design and shipped behavior disagree, follow the real product scope

## Current References

- `job-finder-profile/`
- `job-finder-discovery/`
- `job-finder-review-queue/`
- `job-finder-applications/`
- `job-finder-settings/`
- `job-finder-state-*` (state folders such as `job-finder-state-loading`, `job-finder-state-error`, and `job-finder-state-completed`)
- `tactical-command-design-system/`

## Historical Reference

- superseded design baseline metadata should be consolidated into the nearest directory `README.md` when a standalone note is no longer necessary
