# Core

Owns small cross-cutting helpers and shared result types.

## Rules

- Keep this package lightweight.
- Move domain-specific behavior into the owning package instead of centralizing prematurely.
- Export only reusable primitives with clear consumers.

