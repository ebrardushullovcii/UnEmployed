# Knowledge Base

Owns document ingestion, chunking, indexing, and retrieval.

## Rules

- Keep ingestion pipelines deterministic and testable.
- Treat search and retrieval as shared platform capabilities, not module-owned shortcuts.
- Do not bind this package to a specific model provider.

