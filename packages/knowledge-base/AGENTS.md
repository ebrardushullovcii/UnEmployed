# Knowledge Base

Owns document ingestion, chunking, indexing, and retrieval.

## Rules

- Keep ingestion deterministic and testable
- Treat retrieval as a shared platform capability
- Do not bind this package to one model provider
